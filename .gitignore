# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# Environment / secrets — NEVER commit
.env
.env.*
golazo-auto-API-key.txt
*API-key*.txt
*.key

# OS / editor
.DS_Store
Thumbs.db

# Local render output
/tmp/
out/
gallery.html

# Claude Code agent memory (local only)
memory/

# Archives
files.zip

# NOTE: fonts/ is intentionally NOT ignored — TTF files are required at runtime.
# NOTE: golazo-logo.png is intentionally NOT ignored — the renderer embeds it at runtime.
